package Model.Player;

/**
 * The {@code WorkerType} enum defines the possible types of workers in the Santorini game.
 * The available types are MALE and FEMALE, representing the two genders for each player’s worker.
 * <p>
 * This enum helps to distinguish between the two workers assigned to each player in the game.
 * </p>
 */
public enum WorkerType {

    /**
     * Represents a male worker type.
     */
    MALE,
    /**
     * Represents a female worker type.
     */
    FEMALE

}
